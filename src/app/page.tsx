import Image from "next/image";

export default function Home() {
  return (
    <div className="flex flex-col flex-1 items-center justify-center font-sans bg-primary text-secondary">
      <main className="flex flex-1 w-full">
        Hello world
      </main>
    </div>
  );
}
